# Sync Design Tokens from Specify

Pull the latest design tokens from Specify and update the local files.

检查当前Specify配置并同步最新的设计tokens到本地项目中。

这个命令会：
1. 验证.specifyrc.json配置
2. 检查Specify CLI是否已安装
3. 执行同步操作
4. 显示同步结果